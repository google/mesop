// Temporarily using require instead of ESM imports due to error
// Look into: https://github.com/bazelbuild/rules_nodejs/issues/3277
const ts = require('typescript');

console.log('hello world', ts);
